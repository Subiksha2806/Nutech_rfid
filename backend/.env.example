# MongoDB Connection
# For local MongoDB (requires MongoDB installed locally):
MONGODB_URI=mongodb://127.0.0.1:27017/nutech-warehouse

# Alternative - if using MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/nutech-warehouse

PORT=5000
